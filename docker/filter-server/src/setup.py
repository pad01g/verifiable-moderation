from setuptools import setup

setup(
    name="admin",
    version="version",
    install_requires=[],
    extras_require={
        "develop": []
    },
    entry_points={
        "console_scripts": [
        ],
        "gui_scripts": [
        ]
    }
)