#!/bin/bash

# reader gets filter from filter server


# reader verifies filter by using his own verimod server

# writer writes allowed message to nostr

# reader does not apply filter to message, sees the message

# writer writes forbidden message to nostr

# reader applies filter to message, does not see the message

